import { Box } from '@mui/material';
import { styled } from '@mui/material';

const TableWithPaginationContainer = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  overflow: 'auto',
});

export default TableWithPaginationContainer;
